name: Build Android APK

on:
  push:
    branches: [ main ]
    paths: [ 'android-app/**' ]
  pull_request:
    branches: [ main ]
    paths: [ 'android-app/**' ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Setup Android SDK
      uses: android-actions/setup-android@v3

    - name: Cache Gradle packages
      uses: actions/cache@v4
      with:
        path: |
          ~/.gradle/caches
          ~/.gradle/wrapper
        key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
        restore-keys: |
          ${{ runner.os }}-gradle-

    - name: Grant execute permission for gradlew
      run: chmod +x android-app/gradlew

    - name: Build debug APK
      run: |
        cd android-app
        ./gradlew assembleDebug

    - name: Upload APK Artifact
      uses: actions/upload-artifact@v4
      with:
        name: rbc-wealthpath-debug-apk
        path: android-app/app/build/outputs/apk/debug/app-debug.apk
        retention-days: 30

    - name: Create Release
      if: github.ref == 'refs/heads/main'
      uses: softprops/action-gh-release@v1
      with:
        tag_name: v1.0-${{ github.run_number }}
        name: RBC WealthPath Android v1.0-${{ github.run_number }}
        body: |
          ## 📱 RBC WealthPath Android App
          
          **Download the APK below and install on your Android device!**
          
          ### ✨ Features:
          - 📊 Dashboard with financial health score (8.2/10)
          - 🎯 Goals & Savings with progress tracking  
          - 💬 Floating chat assistant with smart responses
          - 🎨 Professional RBC branding (#006AC2)
          - 📱 Mobile-optimized touch interface
          - 🔄 Bottom navigation with 5 main sections
          
          ### 📲 Installation:
          1. **Download** the `rbc-wealthpath-v1.0.apk` file below
          2. **Enable Unknown Sources** in Android Settings → Security
          3. **Install** by tapping the downloaded APK file
          4. **Launch** the "RBC WealthPath" app
          
          ### 📋 Requirements:
          - **Android 7.0+** (API 24 or higher)
          - **~15MB** storage space
          - **No special permissions** required
          
          ### 🚀 What's New:
          - Complete mobile banking interface
          - Interactive financial assistant
          - Professional card layouts with gradients
          - Real-time goal progress tracking
          - Daily challenges with point rewards
        files: |
          android-app/app/build/outputs/apk/debug/app-debug.apk
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}